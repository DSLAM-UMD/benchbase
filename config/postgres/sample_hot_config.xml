<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>POSTGRES</type>
    <driver>org.postgresql.Driver</driver>
    <url>jdbc:postgresql://localhost:55432/postgres?sslmode=disable&amp;ApplicationName=ycsb&amp;reWriteBatchedInserts=true</url>
    <username>cloud_admin</username>
    <password></password>
    <isolation>TRANSACTION_SERIALIZABLE</isolation>
    <batchsize>128</batchsize>

    <!-- Scalefactor in YCSB is *1000 the number of rows in the USERTABLE-->
    <scalefactor>30</scalefactor>
    
    <!-- Optional: Override the field size for each column in USERTABLE -->
    <fieldSize>8</fieldSize>

    <!-- If not specified, load from 0 -->
    <!-- <loadfrom>0</loadfrom> -->
    <!-- If not specified, load to the maximum number of rows based on the scale factor -->
    <!-- <loadto>30000</loadto> -->

    <numregions>3</numregions>

    <!-- Current region -->
    <region>0</region>

    <!-- Percentage of multiregion transactions -->
    <mrpct>0</mrpct>

    <!-- Number of hot keys per partition -->
    <hot>0</hot>

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>10</time>
            <rate>10000</rate>
            <weights>100,0</weights>
        </work>
    </works>

    <!-- HOT Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>ReadModifyWrite</name>
        </transactiontype>
        <transactiontype>
            <name>RMWLocalRORemote</name>
        </transactiontype>
    </transactiontypes>
</parameters>
