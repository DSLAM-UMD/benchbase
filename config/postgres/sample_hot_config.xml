<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>POSTGRES</type>
    <driver>org.postgresql.Driver</driver>
    <url>jdbc:postgresql://localhost:55433/postgres?sslmode=disable&amp;ApplicationName=ycsb&amp;reWriteBatchedInserts=true</url>
    <username>cloud_admin</username>
    <password></password>
    <isolation>TRANSACTION_SERIALIZABLE</isolation>
    <batchsize>128</batchsize>

    <!-- Scalefactor in YCSB is *1000 the number of rows in the USERTABLE-->
    <scalefactor>1</scalefactor>
    
    <!-- Optional: Override the field size for each column in USERTABLE -->
    <!-- <fieldSize>8</fieldSize> -->

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>30</time>
            <rate>10000</rate>
            <weights>100</weights>
        </work>
    </works>

    <!-- HOT Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>ReadModifyWriteRecord</name>
        </transactiontype>
    </transactiontypes>
</parameters>
